1. execute an action client ask for server to count to a specific target number
2. the action server feedbask the time spent and return the result as the target number
3. execute another node that count the numbers of prime number under the target number
4. wrap the counter node and prime number finding node in a same multi-threaded executor
5. all these three nodes added into same component